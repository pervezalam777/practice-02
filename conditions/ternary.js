let age = 50;
age < 13 ? console.log('your are child') : console.log('your not child');

let result = age < 2 ? 'infant' : age < 5 ? 'toddler': age < 13 ? 'child' : age < 20 ? 'teen': 'adult';
console.log('your are ', result);

/*
if(){

} else {
  if(){

  } else {
    if() {

    }
  }
}
*/